{
  "builds": [
    {
      "src": "backend/shortener-url.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/api(.*)",
      "dest": "/backend/shortener-url.js$1"
    }
  ]
}
